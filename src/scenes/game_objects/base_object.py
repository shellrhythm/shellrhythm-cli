class GameplayObject:
    """Base gameplay object class"""

    def render(self, current_beat:float, dont_draw_list:list) -> list:
        """Prints the object at its position"""
        current_beat += 0
        return dont_draw_list
